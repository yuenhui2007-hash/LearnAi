const express=require('express');const{authenticate}=require('../middleware/auth');const StudyPlan=require('../models/StudyPlan');const router=express.Router();router.post('/plan',authenticate,async(req,res)=>{const{examDate,subjects,hoursPerDay}=req.body;const daysUntil=Math.ceil((new Date(examDate)-Date.now())/86400000);const plan=await StudyPlan.create({userId:req.user.id,examDate,subjects,hoursPerDay,daysUntil,phases:[{week:'1-3',focus:'Weak chapters',priority:'high'},{week:'4-6',focus:'Past papers',priority:'high'},{week:'7-8',focus:'Exam sims',priority:'medium'},{week:'Final',focus:'Targeted revision',priority:'high'}]});res.status(201).json(plan);});router.get('/plans',authenticate,async(req,res)=>res.json(await StudyPlan.find({userId:req.user.id})));router.post('/generate',authenticate,async(req,res)=>res.json({success:true,generated:{type:req.body.type,count:24,format:'interactive'}}));module.exports=router;