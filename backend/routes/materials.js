const express=require('express');const{authenticate}=require('../middleware/auth');const Material=require('../models/Material');const router=express.Router();router.post('/upload',authenticate,async(req,res)=>res.status(201).json({success:true,material:await Material.create({...req.body,userId:req.user.id})}));router.get('/',authenticate,async(req,res)=>res.json(await Material.find({userId:req.user.id})));router.get('/:id',authenticate,async(req,res)=>{const m=await Material.findOne({_id:req.params.id,userId:req.user.id});if(!m)return res.status(404).json({error:'Not found'});res.json(m);});router.delete('/:id',authenticate,async(req,res)=>{await Material.deleteOne({_id:req.params.id,userId:req.user.id});res.json({success:true});});module.exports=router;